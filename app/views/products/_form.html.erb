<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :product_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :product_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :product_price %>
  </div>

  <div class="field">
    <%= form.label :mens_watch %>
    <%= form.text_field :mens_watch, id: :product_mens_watch %>
  </div>

  <div class="field">
    <%= form.label :womens_watch %>
    <%= form.text_field :womens_watch, id: :product_womens_watch %>
  </div>

  <div class="field">
    <%= form.label :kids_watch %>
    <%= form.text_field :kids_watch, id: :product_kids_watch %>
  </div>

  <div class="field">
    <%= form.label :mechanic_watch %>
    <%= form.text_field :mechanic_watch, id: :product_mechanic_watch %>
  </div>

  <div class="field">
    <%= form.label :electronic_watch %>
    <%= form.text_field :electronic_watch, id: :product_electronic_watch %>
  </div>

  <div class="field">
    <%= form.label :available %>
    <%= form.text_field :available, id: :product_available %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.select :category_id, options_from_collection_for_select(Category.all, :id, :category_name, @product.category.id) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
